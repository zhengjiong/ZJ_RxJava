<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zj.example.rxjava" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SchedulersTestActivity"/>
        <activity android:name=".CombineLatestTestActivity"/>
        <activity android:name=".DebounceExampleActivity"/>
        <activity android:name=".DebounceAndSwitchMapExampleActivity"/>
        <activity android:name=".ThrottleFirstAndDebounceExampleActivity"/>
    </application>

</manifest>